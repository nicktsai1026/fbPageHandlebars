<div class="header clearfix container-fluid">
    <div class="col-md-2 column container-fluid">
        {{#profile}}
            <img class="img-thumbnail" alt="140x140" src="{{fbPhoto}}" />
            <p>{{fbName}}</p>
            <p>Total liked pages: {{totalPage}}</p>
        {{/profile}}

    </div>
    {{#profile}}
    <a href="/home">
    <div class="col-md-8 column">
            <h1>Welcome {{fbName}}!</h1>
    </div>
    </a>
    <div class="col-md-2 column">
        <a href="/checkFriends">
            <button class="btn btn-default" type="button">
                See your "common likes"
            </button>
        </a>
        <button class="btn btn-default" type="button">
            Share it to your friends
        </button>
    </div>
    <div class="progress">
        <div class="progress-bar" role="progressbar" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100" style="width: 100%">
        </div>
    </div>
    {{/profile}}
</div>
{{!--
<div class="profile">
    {{#profile}}
    <img src="{{fbPhoto}}" alt="personal photo">
    <h4>{{fbName}}</h4>
    {{/profile}} {{#totalPage}}
    <p>Total pages: {{total}}</p>
    {{/totalPage}}
    <a href="/checkFriends"><button class="btn btn-info">Find your friends who liked with you!</button></a>
</div> --}}